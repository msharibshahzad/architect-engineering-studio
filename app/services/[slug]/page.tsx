import {services} from "@/lib/data";import {notFound} from "next/navigation";export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}export default async function ServicePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const service=services.find(s=>s.slug===slug);if(!service)notFound();return <main className="min-h-screen bg-[#0b0b0a] pt-32 pb-24"><div className="container-x"><a href="/" className="text-xs uppercase tracking-widest text-white/50">← Back home</a><p className="mt-20 text-xs uppercase tracking-widest text-[#b08a55]">{service.number} / Service</p><h1 className="display mt-5 text-6xl md:text-8xl">{service.title}</h1><p className="mt-8 max-w-2xl text-lg leading-8 text-white/60">{service.description}</p><div className="mt-14 grid gap-4 md:grid-cols-3">{service.features.map(f=><div key={f} className="glass p-7">{f}</div>)}</div></div></main>}